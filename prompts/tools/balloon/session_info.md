### session_info

Get information about the current session including context usage, fork tree navigation (merged_from, forked_to, merged_to), and parent chain.

**Parameters:** None

Essential for understanding session state and traversing the fork tree.
