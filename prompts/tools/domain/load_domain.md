### load_domain

Load a domain plugin to gain new capabilities.

**Parameters:**
- `domain_id` (required): ID of the domain to load (e.g., "chess", "kanban")

Once loaded, the domain's tools become available and its documentation is added to your context.

