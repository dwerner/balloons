### browser_set_input

Set an input field by its index from `browser_inputs`.

**Parameters:**
- `index`: Input index (0-based, from `browser_inputs` discovery)
- `value`: Value to set

This is often more reliable than CSS selectors since it uses the actual discovered inputs.
