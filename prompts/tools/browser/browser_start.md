### browser_start

Start a new browser session.

**Parameters:**
- `browser_type`: "firefox" (default) or "chrome"
- `headless`: Run without visible window (default: true)

Call this before any other browser operations. Returns a browser ID for reference.
