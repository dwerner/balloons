### browser_links

Discover all links on the current page.

**Parameters:**
- `limit`: Maximum number of links to return (optional)

Returns a JSON array of link info objects with:
- `index`: Link index
- `text`: Link text
- `href`: Link URL
