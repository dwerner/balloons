### browser_click

Click an element by CSS selector.

**Parameters:**
- `selector`: CSS selector for the element (e.g., "#submit", ".btn-primary", "button[type='submit']")

For clicking buttons by index from `browser_buttons`, use `browser_click_button` instead.
