### browser_click_button

Click a button by its index from `browser_buttons`.

**Parameters:**
- `index`: Button index (0-based, from `browser_buttons` discovery)

This is often more reliable than CSS selectors since it uses the actual discovered buttons.
