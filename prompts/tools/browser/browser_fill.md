### browser_fill

Fill an input field by CSS selector. Clears existing content first.

**Parameters:**
- `selector`: CSS selector for the input element
- `text`: Text to enter

For filling by index from `browser_inputs`, use `browser_set_input` instead.
