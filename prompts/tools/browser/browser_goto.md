### browser_goto

Navigate to a URL.

**Parameters:**
- `url`: The URL to navigate to (must be fully qualified, e.g., "https://example.com")

After navigation, use `browser_see` to understand the page structure.
