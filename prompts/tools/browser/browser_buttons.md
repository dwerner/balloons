### browser_buttons

Discover all button elements on the current page.

Returns a JSON array of button info objects with:
- `index`: Use this with `browser_click_button`
- `text`: Button text content
- `type`: Button type (submit, button, reset)
